package cintlex.deadplayerchunkupdates;
import net.neoforged.bus.api.IEventBus;
import net.neoforged.fml.common.Mod;

@Mod("deadplayerchunkupdates")
public class DeadPlayerChunkUpdates {
    public DeadPlayerChunkUpdates(IEventBus modEventBus) {
    }
}